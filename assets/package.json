{
  "name": "oban_ui-assets",
  "version": "0.1.0",
  "private": true,
  "description": "Source assets for the oban_ui Elixir library. Built via mix tasks; npm is not used by hosts.",
  "scripts": {
    "build": "echo 'use mix assets.build instead'"
  },
  "dependencies": {
    "phoenix": "file:../deps/phoenix",
    "phoenix_html": "file:../deps/phoenix_html",
    "phoenix_live_view": "file:../deps/phoenix_live_view"
  }
}
